# قاعدة البيانات (Neon PostgreSQL)
# محلياً: postgresql://postgres:postgres@127.0.0.1:5432/notes_db
# Netlify + Neon: postgresql://user:password@host/dbname?sslmode=require
DATABASE_URL=

# بيئة التطوير
NODE_ENV=development

# رابط التطبيق
NEXT_PUBLIC_APP_URL=http://localhost:3000
